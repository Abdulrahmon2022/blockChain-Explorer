# Sepolia JSON-RPC endpoint used by the data layer (lib/rpc.ts).
# The default public node works but is rate limited — swap in an Alchemy or
# Infura URL if you start seeing 429s.
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
